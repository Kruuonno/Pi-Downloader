---
version: "2.1"
services:
  sonarr:
    image: ghcr.io/linuxserver/sonarr
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Los_Angeles
    volumes:
      - /mnt/dietpi_userdata/config/sonarr:/config
      - /mnt/dietpi_userdata/tvseries:/tv
      - /mnt/dietpi_userdata/downloadclient-downloads:/downloads
    ports:
      - 8989:8989
    restart: unless-stopped
